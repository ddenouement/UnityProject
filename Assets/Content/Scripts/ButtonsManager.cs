using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ButtonsManager : MonoBehaviour {

    /*public HeroRabbitGood HeroRabbit;
   public MyButton resume = null; 
    public MyButton music = null;    
    public MyButton sounds = null;
    public SettingsPanel sp = null;
    void Awake()
    {
        //   closeSettings.signalOnClick.AddListener(this.PlayBtnClicked); 
        resume.signalOnClick.AddListener(this.resumeClicked);
    }
   
    public void resumeClicked()
    {
        Destroy(sp);
        HeroRabbit.isPaused = false;
     //   Debug.Log("here");
    }
    // Use this for initialization
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }*/
}
